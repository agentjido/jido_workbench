<%= base_prompt %>

=== PASS 1: STRUCTURE PLANNING ===
Produce only the structure plan (no final prose).

Requirements:
- Section order must satisfy the authoring contract exactly.
- Plan where code blocks appear and what each block proves.
- Plan internal links and citations needed to pass audit gates.
- Make evidence expectations explicit in `citation_plan`.
- Include concise consistency rules so pass 2 keeps stable voice and terminology.

Do not write full markdown page text in this pass.
Return only the structured object fields required by the schema.
