# Epic 5 - Top 20 LiveView Example Expansion

### ST-EX-001 Build example `address-normalization-agent` (Address Normalization Agent)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.AddressNormalizationAgent` and the actions listed in `source_files` for `address-normalization-agent`.
- Implement LiveView module `AgentJidoWeb.Examples.AddressNormalizationAgentLive` at `lib/agent_jido_web/examples/address_normalization_agent_live.ex`.
- Keep `priv/examples/address-normalization-agent.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `address-normalization-agent`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/address-normalization-agent` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.AddressNormalizationAgentLive` without runtime errors.
- Demo includes at least one interaction proving `Action contracts and validation`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/address-normalization-agent?tab=explanation`.
- LiveView render test for `/examples/address-normalization-agent?tab=source`.
- LiveView render and interaction test for `/examples/address-normalization-agent?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("address-normalization-agent")` returns expected metadata.

### ST-EX-002 Build example `budget-guardrail-agent` (Budget Guardrail Agent)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.BudgetGuardrailAgent` and the actions listed in `source_files` for `budget-guardrail-agent`.
- Implement LiveView module `AgentJidoWeb.Examples.BudgetGuardrailAgentLive` at `lib/agent_jido_web/examples/budget_guardrail_agent_live.ex`.
- Keep `priv/examples/budget-guardrail-agent.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `budget-guardrail-agent`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/budget-guardrail-agent` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.BudgetGuardrailAgentLive` without runtime errors.
- Demo includes at least one interaction proving `Action contracts and validation`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/budget-guardrail-agent?tab=explanation`.
- LiveView render test for `/examples/budget-guardrail-agent?tab=source`.
- LiveView render and interaction test for `/examples/budget-guardrail-agent?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("budget-guardrail-agent")` returns expected metadata.

### ST-EX-003 Build example `capacity-quota-tracker-agent` (Capacity Quota Tracker Agent)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.CapacityQuotaTrackerAgent` and the actions listed in `source_files` for `capacity-quota-tracker-agent`.
- Implement LiveView module `AgentJidoWeb.Examples.CapacityQuotaTrackerAgentLive` at `lib/agent_jido_web/examples/capacity_quota_tracker_agent_live.ex`.
- Keep `priv/examples/capacity-quota-tracker-agent.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `capacity-quota-tracker-agent`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/capacity-quota-tracker-agent` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.CapacityQuotaTrackerAgentLive` without runtime errors.
- Demo includes at least one interaction proving `Action contracts and validation`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/capacity-quota-tracker-agent?tab=explanation`.
- LiveView render test for `/examples/capacity-quota-tracker-agent?tab=source`.
- LiveView render and interaction test for `/examples/capacity-quota-tracker-agent?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("capacity-quota-tracker-agent")` returns expected metadata.

### ST-EX-004 Build example `cart-value-calculator-agent` (Cart Value Calculator Agent)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.CartValueCalculatorAgent` and the actions listed in `source_files` for `cart-value-calculator-agent`.
- Implement LiveView module `AgentJidoWeb.Examples.CartValueCalculatorAgentLive` at `lib/agent_jido_web/examples/cart_value_calculator_agent_live.ex`.
- Keep `priv/examples/cart-value-calculator-agent.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `cart-value-calculator-agent`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/cart-value-calculator-agent` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.CartValueCalculatorAgentLive` without runtime errors.
- Demo includes at least one interaction proving `Action contracts and validation`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/cart-value-calculator-agent?tab=explanation`.
- LiveView render test for `/examples/cart-value-calculator-agent?tab=source`.
- LiveView render and interaction test for `/examples/cart-value-calculator-agent?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("cart-value-calculator-agent")` returns expected metadata.

### ST-EX-005 Build example `feature-flag-audit-agent` (Feature Flag Audit Agent)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.FeatureFlagAuditAgent` and the actions listed in `source_files` for `feature-flag-audit-agent`.
- Implement LiveView module `AgentJidoWeb.Examples.FeatureFlagAuditAgentLive` at `lib/agent_jido_web/examples/feature_flag_audit_agent_live.ex`.
- Keep `priv/examples/feature-flag-audit-agent.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `feature-flag-audit-agent`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/feature-flag-audit-agent` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.FeatureFlagAuditAgentLive` without runtime errors.
- Demo includes at least one interaction proving `Action contracts and validation`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/feature-flag-audit-agent?tab=explanation`.
- LiveView render test for `/examples/feature-flag-audit-agent?tab=source`.
- LiveView render and interaction test for `/examples/feature-flag-audit-agent?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("feature-flag-audit-agent")` returns expected metadata.

### ST-EX-006 Build example `liveview-checkout-recovery-coach` (LiveView Checkout Recovery Coach)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.LiveviewCheckoutRecoveryCoachAgent` and the actions listed in `source_files` for `liveview-checkout-recovery-coach`.
- Implement LiveView module `AgentJidoWeb.Examples.LiveviewCheckoutRecoveryCoachLive` at `lib/agent_jido_web/examples/liveview_checkout_recovery_coach_live.ex`.
- Keep `priv/examples/liveview-checkout-recovery-coach.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `liveview-checkout-recovery-coach`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/liveview-checkout-recovery-coach` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.LiveviewCheckoutRecoveryCoachLive` without runtime errors.
- Demo includes at least one interaction proving `LiveView interaction patterns`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/liveview-checkout-recovery-coach?tab=explanation`.
- LiveView render test for `/examples/liveview-checkout-recovery-coach?tab=source`.
- LiveView render and interaction test for `/examples/liveview-checkout-recovery-coach?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("liveview-checkout-recovery-coach")` returns expected metadata.

### ST-EX-007 Build example `meeting-prep-briefing-console` (Meeting Prep Briefing Console)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.MeetingPrepBriefingConsoleAgent` and the actions listed in `source_files` for `meeting-prep-briefing-console`.
- Implement LiveView module `AgentJidoWeb.Examples.MeetingPrepBriefingConsoleLive` at `lib/agent_jido_web/examples/meeting_prep_briefing_console_live.ex`.
- Keep `priv/examples/meeting-prep-briefing-console.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `meeting-prep-briefing-console`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/meeting-prep-briefing-console` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.MeetingPrepBriefingConsoleLive` without runtime errors.
- Demo includes at least one interaction proving `AI/tool-use integration`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/meeting-prep-briefing-console?tab=explanation`.
- LiveView render test for `/examples/meeting-prep-briefing-console?tab=source`.
- LiveView render and interaction test for `/examples/meeting-prep-briefing-console?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("meeting-prep-briefing-console")` returns expected metadata.

### ST-EX-008 Build example `order-approval-to-fulfillment-chain` (Order Approval to Fulfillment Chain)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.OrderApprovalToFulfillmentChainAgent` and the actions listed in `source_files` for `order-approval-to-fulfillment-chain`.
- Implement LiveView module `AgentJidoWeb.Examples.OrderApprovalToFulfillmentChainLive` at `lib/agent_jido_web/examples/order_approval_to_fulfillment_chain_live.ex`.
- Keep `priv/examples/order-approval-to-fulfillment-chain.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `order-approval-to-fulfillment-chain`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/order-approval-to-fulfillment-chain` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.OrderApprovalToFulfillmentChainLive` without runtime errors.
- Demo includes at least one interaction proving `Signals and routing`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/order-approval-to-fulfillment-chain?tab=explanation`.
- LiveView render test for `/examples/order-approval-to-fulfillment-chain?tab=source`.
- LiveView render and interaction test for `/examples/order-approval-to-fulfillment-chain?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("order-approval-to-fulfillment-chain")` returns expected metadata.

### ST-EX-009 Build example `ticket-triage-swarm-coordinator` (Ticket Triage Swarm Coordinator)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.TicketTriageSwarmCoordinatorAgent` and the actions listed in `source_files` for `ticket-triage-swarm-coordinator`.
- Implement LiveView module `AgentJidoWeb.Examples.TicketTriageSwarmCoordinatorLive` at `lib/agent_jido_web/examples/ticket_triage_swarm_coordinator_live.ex`.
- Keep `priv/examples/ticket-triage-swarm-coordinator.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `ticket-triage-swarm-coordinator`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/ticket-triage-swarm-coordinator` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.TicketTriageSwarmCoordinatorLive` without runtime errors.
- Demo includes at least one interaction proving `Directives (emit/schedule)`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/ticket-triage-swarm-coordinator?tab=explanation`.
- LiveView render test for `/examples/ticket-triage-swarm-coordinator?tab=source`.
- LiveView render and interaction test for `/examples/ticket-triage-swarm-coordinator?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("ticket-triage-swarm-coordinator")` returns expected metadata.

### ST-EX-010 Build example `document-grounded-policy-qna-agent` (Document-Grounded Policy QnA Agent)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.DocumentGroundedPolicyQnaAgent` and the actions listed in `source_files` for `document-grounded-policy-qna-agent`.
- Implement LiveView module `AgentJidoWeb.Examples.DocumentGroundedPolicyQnaAgentLive` at `lib/agent_jido_web/examples/document_grounded_policy_qna_agent_live.ex`.
- Keep `priv/examples/document-grounded-policy-qna-agent.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `document-grounded-policy-qna-agent`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/document-grounded-policy-qna-agent` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.DocumentGroundedPolicyQnaAgentLive` without runtime errors.
- Demo includes at least one interaction proving `Action contracts and validation`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/document-grounded-policy-qna-agent?tab=explanation`.
- LiveView render test for `/examples/document-grounded-policy-qna-agent?tab=source`.
- LiveView render and interaction test for `/examples/document-grounded-policy-qna-agent?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("document-grounded-policy-qna-agent")` returns expected metadata.

### ST-EX-011 Build example `pr-review-suggestion-agent` (PR Review Suggestion Agent)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.PrReviewSuggestionAgent` and the actions listed in `source_files` for `pr-review-suggestion-agent`.
- Implement LiveView module `AgentJidoWeb.Examples.PrReviewSuggestionAgentLive` at `lib/agent_jido_web/examples/pr_review_suggestion_agent_live.ex`.
- Keep `priv/examples/pr-review-suggestion-agent.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `pr-review-suggestion-agent`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/pr-review-suggestion-agent` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.PrReviewSuggestionAgentLive` without runtime errors.
- Demo includes at least one interaction proving `AI/tool-use integration`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/pr-review-suggestion-agent?tab=explanation`.
- LiveView render test for `/examples/pr-review-suggestion-agent?tab=source`.
- LiveView render and interaction test for `/examples/pr-review-suggestion-agent?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("pr-review-suggestion-agent")` returns expected metadata.

### ST-EX-012 Build example `telemetry-slo-budget-sentinel` (Telemetry SLO Budget Sentinel)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.TelemetrySloBudgetSentinelAgent` and the actions listed in `source_files` for `telemetry-slo-budget-sentinel`.
- Implement LiveView module `AgentJidoWeb.Examples.TelemetrySloBudgetSentinelLive` at `lib/agent_jido_web/examples/telemetry_slo_budget_sentinel_live.ex`.
- Keep `priv/examples/telemetry-slo-budget-sentinel.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `telemetry-slo-budget-sentinel`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/telemetry-slo-budget-sentinel` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.TelemetrySloBudgetSentinelLive` without runtime errors.
- Demo includes at least one interaction proving `Telemetry/observability`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/telemetry-slo-budget-sentinel?tab=explanation`.
- LiveView render test for `/examples/telemetry-slo-budget-sentinel?tab=source`.
- LiveView render and interaction test for `/examples/telemetry-slo-budget-sentinel?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("telemetry-slo-budget-sentinel")` returns expected metadata.

### ST-EX-013 Build example `csv-import-validator-agent` (CSV Import Validator Agent)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.CsvImportValidatorAgent` and the actions listed in `source_files` for `csv-import-validator-agent`.
- Implement LiveView module `AgentJidoWeb.Examples.CsvImportValidatorAgentLive` at `lib/agent_jido_web/examples/csv_import_validator_agent_live.ex`.
- Keep `priv/examples/csv-import-validator-agent.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `csv-import-validator-agent`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/csv-import-validator-agent` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.CsvImportValidatorAgentLive` without runtime errors.
- Demo includes at least one interaction proving `Action contracts and validation`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/csv-import-validator-agent?tab=explanation`.
- LiveView render test for `/examples/csv-import-validator-agent?tab=source`.
- LiveView render and interaction test for `/examples/csv-import-validator-agent?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("csv-import-validator-agent")` returns expected metadata.

### ST-EX-014 Build example `catalog-variant-consistency-agent` (Catalog Variant Consistency Agent)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.CatalogVariantConsistencyAgent` and the actions listed in `source_files` for `catalog-variant-consistency-agent`.
- Implement LiveView module `AgentJidoWeb.Examples.CatalogVariantConsistencyAgentLive` at `lib/agent_jido_web/examples/catalog_variant_consistency_agent_live.ex`.
- Keep `priv/examples/catalog-variant-consistency-agent.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `catalog-variant-consistency-agent`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/catalog-variant-consistency-agent` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.CatalogVariantConsistencyAgentLive` without runtime errors.
- Demo includes at least one interaction proving `Action contracts and validation`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/catalog-variant-consistency-agent?tab=explanation`.
- LiveView render test for `/examples/catalog-variant-consistency-agent?tab=source`.
- LiveView render and interaction test for `/examples/catalog-variant-consistency-agent?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("catalog-variant-consistency-agent")` returns expected metadata.

### ST-EX-015 Build example `changelog-entry-linter-agent` (Changelog Entry Linter Agent)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.ChangelogEntryLinterAgent` and the actions listed in `source_files` for `changelog-entry-linter-agent`.
- Implement LiveView module `AgentJidoWeb.Examples.ChangelogEntryLinterAgentLive` at `lib/agent_jido_web/examples/changelog_entry_linter_agent_live.ex`.
- Keep `priv/examples/changelog-entry-linter-agent.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `changelog-entry-linter-agent`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/changelog-entry-linter-agent` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.ChangelogEntryLinterAgentLive` without runtime errors.
- Demo includes at least one interaction proving `Action contracts and validation`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/changelog-entry-linter-agent?tab=explanation`.
- LiveView render test for `/examples/changelog-entry-linter-agent?tab=source`.
- LiveView render and interaction test for `/examples/changelog-entry-linter-agent?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("changelog-entry-linter-agent")` returns expected metadata.

### ST-EX-016 Build example `dependency-license-classifier-agent` (Dependency License Classifier Agent)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.DependencyLicenseClassifierAgent` and the actions listed in `source_files` for `dependency-license-classifier-agent`.
- Implement LiveView module `AgentJidoWeb.Examples.DependencyLicenseClassifierAgentLive` at `lib/agent_jido_web/examples/dependency_license_classifier_agent_live.ex`.
- Keep `priv/examples/dependency-license-classifier-agent.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `dependency-license-classifier-agent`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/dependency-license-classifier-agent` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.DependencyLicenseClassifierAgentLive` without runtime errors.
- Demo includes at least one interaction proving `Agent state/schema fundamentals`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/dependency-license-classifier-agent?tab=explanation`.
- LiveView render test for `/examples/dependency-license-classifier-agent?tab=source`.
- LiveView render and interaction test for `/examples/dependency-license-classifier-agent?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("dependency-license-classifier-agent")` returns expected metadata.

### ST-EX-017 Build example `incident-timeline-narrator-agent` (Incident Timeline Narrator Agent)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.IncidentTimelineNarratorAgent` and the actions listed in `source_files` for `incident-timeline-narrator-agent`.
- Implement LiveView module `AgentJidoWeb.Examples.IncidentTimelineNarratorAgentLive` at `lib/agent_jido_web/examples/incident_timeline_narrator_agent_live.ex`.
- Keep `priv/examples/incident-timeline-narrator-agent.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `incident-timeline-narrator-agent`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/incident-timeline-narrator-agent` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.IncidentTimelineNarratorAgentLive` without runtime errors.
- Demo includes at least one interaction proving `Telemetry/observability`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/incident-timeline-narrator-agent?tab=explanation`.
- LiveView render test for `/examples/incident-timeline-narrator-agent?tab=source`.
- LiveView render and interaction test for `/examples/incident-timeline-narrator-agent?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("incident-timeline-narrator-agent")` returns expected metadata.

### ST-EX-018 Build example `release-notes-drafting-agent` (Release Notes Drafting Agent)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.ReleaseNotesDraftingAgent` and the actions listed in `source_files` for `release-notes-drafting-agent`.
- Implement LiveView module `AgentJidoWeb.Examples.ReleaseNotesDraftingAgentLive` at `lib/agent_jido_web/examples/release_notes_drafting_agent_live.ex`.
- Keep `priv/examples/release-notes-drafting-agent.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `release-notes-drafting-agent`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/release-notes-drafting-agent` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.ReleaseNotesDraftingAgentLive` without runtime errors.
- Demo includes at least one interaction proving `Telemetry/observability`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/release-notes-drafting-agent?tab=explanation`.
- LiveView render test for `/examples/release-notes-drafting-agent?tab=source`.
- LiveView render and interaction test for `/examples/release-notes-drafting-agent?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("release-notes-drafting-agent")` returns expected metadata.

### ST-EX-019 Build example `async-payment-retry-orchestrator` (Async Payment Retry Orchestrator)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.AsyncPaymentRetryOrchestratorAgent` and the actions listed in `source_files` for `async-payment-retry-orchestrator`.
- Implement LiveView module `AgentJidoWeb.Examples.AsyncPaymentRetryOrchestratorLive` at `lib/agent_jido_web/examples/async_payment_retry_orchestrator_live.ex`.
- Keep `priv/examples/async-payment-retry-orchestrator.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `async-payment-retry-orchestrator`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/async-payment-retry-orchestrator` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.AsyncPaymentRetryOrchestratorLive` without runtime errors.
- Demo includes at least one interaction proving `Signals and routing`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/async-payment-retry-orchestrator?tab=explanation`.
- LiveView render test for `/examples/async-payment-retry-orchestrator?tab=source`.
- LiveView render and interaction test for `/examples/async-payment-retry-orchestrator?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("async-payment-retry-orchestrator")` returns expected metadata.

### ST-EX-020 Build example `dead-letter-reprocessor-workflow` (Dead Letter Reprocessor Workflow)
#### Epic
Epic 5 - Top 20 LiveView Example Expansion
#### Dependencies
- None
#### Scope
- Implement agent module `AgentJido.Demos.DeadLetterReprocessorWorkflowAgent` and the actions listed in `source_files` for `dead-letter-reprocessor-workflow`.
- Implement LiveView module `AgentJidoWeb.Examples.DeadLetterReprocessorWorkflowLive` at `lib/agent_jido_web/examples/dead_letter_reprocessor_workflow_live.ex`.
- Keep `priv/examples/dead-letter-reprocessor-workflow.md` aligned with final implementation details and source file list.
- Ensure the interactive demo is deterministic and tied to the primary learning goal.
#### Out of Scope
- Implementing any other top-20 example besides `dead-letter-reprocessor-workflow`.
- Production hardening beyond the scope of the example itself.
#### Acceptance Criteria
- `/examples/dead-letter-reprocessor-workflow` renders explanation and source tabs correctly.
- Demo tab mounts `AgentJidoWeb.Examples.DeadLetterReprocessorWorkflowLive` without runtime errors.
- Demo includes at least one interaction proving `Signals and routing`.
- Example frontmatter `source_files` resolves to the implemented files.
#### Test Cases
- LiveView render test for `/examples/dead-letter-reprocessor-workflow?tab=explanation`.
- LiveView render test for `/examples/dead-letter-reprocessor-workflow?tab=source`.
- LiveView render and interaction test for `/examples/dead-letter-reprocessor-workflow?tab=demo`.
- Registry check: `AgentJido.Examples.get_example!("dead-letter-reprocessor-workflow")` returns expected metadata.
