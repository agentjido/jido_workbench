[Back to index](/Users/Pascal/code/agentjido/jido_run/specs/planning/credo/README.md)

## Relevant Shared APIs / Interfaces

- `mix credo --strict`
- documentation-bearing modules under `lib/**`
- parser and generator modules under `lib/agent_jido/content_gen/**`
- line-length, moduledoc, and readability rules enforced by Credo

## Relevant Assumptions / Defaults

- This phase targets readability-only warnings: missing `@moduledoc`, line length, predicate naming, explicit `try`, sigil suggestions, and single-clause `with` readability findings.
- The output should stay behaviorally neutral unless a readability rewrite naturally simplifies control flow.
- Modules generated by Phoenix auth scaffolding still need to meet the repo standard if they remain tracked code.

[ ] 2 Phase 2 - Readability and Documentation Hygiene
  Remove readability friction that makes the code harder to scan, document, and maintain even when the behavior is already correct.

  [ ] 2.1 Section - Missing Module Documentation
    Add or intentionally suppress module documentation where tracked modules currently fail the repo standard.

    [ ] 2.1.1 Task - Add `@moduledoc` to tracked application modules
      Fill the documented gaps in accounts, auth, icon, SEO, and code-example modules.

      [ ] 2.1.1.1 Subtask - Write concise `@moduledoc` values that explain each module’s role rather than repeating the module name.
      [ ] 2.1.1.2 Subtask - Re-check the files after edits so new line-length or formatting issues are not introduced.

  [ ] 2.2 Section - Readability Rewrite Rules
    Apply the repeated readability rewrites that do not change semantics but make intent clearer to future maintainers.

    [ ] 2.2.1 Task - Replace single-clause `with` expressions with `case`
      Convert the `15` single-clause `with` usages into the direct control-flow form Credo expects.

      [ ] 2.2.1.1 Subtask - Start with `content_assistant`, `content_gen`, and LiveView refresh helpers where the rule repeats most often.
      [ ] 2.2.1.2 Subtask - Keep pattern matching explicit so the resulting `case` blocks improve readability instead of merely satisfying the check.

    [ ] 2.2.2 Task - Replace explicit `try` where implicit `try` is sufficient
      Remove the readability-only `try` wrappers where the language already supplies the same rescue semantics.

      [ ] 2.2.2.1 Subtask - Clean the parser, writer, admin content generator, and ops-service sites flagged by Credo.
      [ ] 2.2.2.2 Subtask - Confirm the resulting code still preserves error normalization and caller-facing behavior.

    [ ] 2.2.3 Task - Fix predicate naming and string literal readability
      Rename predicate helpers and convert outlier string literals to sigils where Credo calls out readability debt directly.

      [ ] 2.2.3.1 Subtask - Rename `is_nil_or_empty`-style helpers to question-mark predicates and update call sites atomically.
      [ ] 2.2.3.2 Subtask - Apply the quoted-string sigil cleanup only where it materially improves readability.

  [ ] 2.3 Section - Line Length Cleanup
    Reduce line-length violations in the most frequently touched content, analytics, and admin code paths.

    [ ] 2.3.1 Task - Wrap long expressions without harming scanability
      Break long lines into shapes that improve reviewability instead of merely moving commas to satisfy the formatter.

      [ ] 2.3.1.1 Subtask - Prioritize long lines in `content_gen`, `content_ontology`, `github_stars_tracker`, and admin tests because they recur in active work.
      [ ] 2.3.1.2 Subtask - Favor extracting small helpers or intermediate values when wrapping alone would make code harder to read.

  [ ] 2.4 Section - Phase 2 Integration Tests
    Verify that readability and documentation cleanup removes the targeted findings while keeping behavior intact.

    [ ] 2.4.1 Task - Add Phase 2 verification coverage
      Confirm the readability family has dropped and no regressions were introduced in touched code.

      [ ] 2.4.1.1 Subtask - Run `mix format --check-formatted` and `mix compile --warnings-as-errors` after each readability batch.
      [ ] 2.4.1.2 Subtask - Run `mix credo --strict` and confirm the missing-moduledoc, line-length, single-clause-`with`, and explicit-`try` counts fall as planned.
